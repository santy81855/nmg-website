import styles from "./style.module.css";

const Contact = () => {
    return (
        <section className={styles.container}>
            <p>hi</p>
        </section>
    );
};

export default Contact;
